# Experimental Features

This directory is for experimental renderers, UI components, and effects.

## Rule
Nothing inside the `experimental/` directory may be imported into the main application codebase without passing a professional audit and architecture review.
